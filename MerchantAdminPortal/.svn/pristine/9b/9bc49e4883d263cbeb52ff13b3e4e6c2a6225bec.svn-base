package com.npst.upi.portal.merchant.utility;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

//import com.rabbitmq.client.ConnectionFactory;

public class RmqConnFactory {
	/*
	 * private static final Logger log =
	 * LoggerFactory.getLogger(RmqConnFactory.class); private final static
	 * ConnectionFactory rmqConFactory; static { rmqConFactory = new
	 * ConnectionFactory(); try { String host = Utility.getProperty("QHOST"); String
	 * user = Utility.getProperty("QUSERNAME"); String pass =
	 * Utility.getProperty("QPASSWORD"); rmqConFactory.setHost(host);
	 * rmqConFactory.setUsername(user); rmqConFactory.setPassword(pass);
	 * log.debug("Creating new ConnectionFactory for RabbitMQ"); } catch (Exception
	 * e) { log.error("error in creating RMQ ConnectionFactory {}", e);
	 * System.exit(1); }
	 * 
	 * }
	 * 
	 * public static ConnectionFactory getConnectionFactory() { return
	 * rmqConFactory; }
	 */}
